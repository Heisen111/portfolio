import type { FinalJourney } from "../types";

/** 06 — THE NEXT JOURNEY closing-script copy. Links live in `src/content/socials.ts`. */
export const finalJourney: FinalJourney = {
  closing: "THE JOURNEY CONTINUES.",
};