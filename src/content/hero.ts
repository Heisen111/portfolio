import type { HeroCopy } from "../types";

/**
 * Hero copy. The monogram, title line, and quotes come from
 * `src/content/profile.ts` (quote copy still pending — do not invent).
 */
export const heroCopy: HeroCopy = {
  scrollCue: "SCROLL",
};
